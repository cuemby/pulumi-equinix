// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a resource to attach device ports to VLANs.
 *
 * Device and VLAN must be in the same facility.
 *
 * If you need this resource to add the port back to bond on removal, set `forceBond = true`.
 *
 * To learn more about Layer 2 networking in Equinix Metal, refer to
 *
 * * <https://metal.equinix.com/developers/docs/networking/layer2/>
 * * <https://metal.equinix.com/developers/docs/networking/layer2-configs/>
 *
 * ## Example Usage
 * ### Hybrid network type
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as equinix from "@cuemby/equinix";
 *
 * const testMetalVlan = new equinix.MetalVlan("testMetalVlan", {
 *     description: "VLAN in New Jersey",
 *     facility: "ny5",
 *     projectId: local.project_id,
 * });
 * const testMetalDevice = new equinix.MetalDevice("testMetalDevice", {
 *     hostname: "test",
 *     plan: "c3.small.x86",
 *     facilities: ["ny5"],
 *     operatingSystem: "ubuntu_20_04",
 *     billingCycle: "hourly",
 *     projectId: local.project_id,
 * });
 * const testMetalDeviceNetworkType = new equinix.MetalDeviceNetworkType("testMetalDeviceNetworkType", {
 *     deviceId: testMetalDevice.id,
 *     type: "hybrid",
 * });
 * const testMetalPortVlanAttachment = new equinix.MetalPortVlanAttachment("testMetalPortVlanAttachment", {
 *     deviceId: testMetalDeviceNetworkType.id,
 *     portName: "eth1",
 *     vlanVnid: testMetalVlan.vxlan,
 * });
 * ```
 * ### Layer 2 network
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as equinix from "@cuemby/equinix";
 *
 * const testMetalDevice = new equinix.MetalDevice("testMetalDevice", {
 *     hostname: "test",
 *     plan: "c3.small.x86",
 *     facilities: ["ny5"],
 *     operatingSystem: "ubuntu_20_04",
 *     billingCycle: "hourly",
 *     projectId: local.project_id,
 * });
 * const testMetalDeviceNetworkType = new equinix.MetalDeviceNetworkType("testMetalDeviceNetworkType", {
 *     deviceId: testMetalDevice.id,
 *     type: "layer2-individual",
 * });
 * const test1MetalVlan = new equinix.MetalVlan("test1MetalVlan", {
 *     description: "VLAN in New Jersey",
 *     facility: "ny5",
 *     projectId: local.project_id,
 * });
 * const test2MetalVlan = new equinix.MetalVlan("test2MetalVlan", {
 *     description: "VLAN in New Jersey",
 *     facility: "ny5",
 *     projectId: local.project_id,
 * });
 * const test1MetalPortVlanAttachment = new equinix.MetalPortVlanAttachment("test1MetalPortVlanAttachment", {
 *     deviceId: testMetalDeviceNetworkType.id,
 *     vlanVnid: test1MetalVlan.vxlan,
 *     portName: "eth1",
 * });
 * const test2MetalPortVlanAttachment = new equinix.MetalPortVlanAttachment("test2MetalPortVlanAttachment", {
 *     deviceId: testMetalDeviceNetworkType.id,
 *     vlanVnid: test2MetalVlan.vxlan,
 *     portName: "eth1",
 *     native: true,
 * }, {
 *     dependsOn: ["equinix_metal_port_vlan_attachment.test1"],
 * });
 * ```
 * ## Attribute Referece
 *
 * In addition to all arguments above, the following attributes are exported:
 *
 * * `id` - UUID of device port used in the assignment.
 * * `vlanId` - UUID of VLAN API resource.
 * * `portId` - UUID of device port.
 */
export class MetalPortVlanAttachment extends pulumi.CustomResource {
    /**
     * Get an existing MetalPortVlanAttachment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: MetalPortVlanAttachmentState, opts?: pulumi.CustomResourceOptions): MetalPortVlanAttachment {
        return new MetalPortVlanAttachment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'equinix:index/metalPortVlanAttachment:MetalPortVlanAttachment';

    /**
     * Returns true if the given object is an instance of MetalPortVlanAttachment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is MetalPortVlanAttachment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === MetalPortVlanAttachment.__pulumiType;
    }

    /**
     * ID of device to be assigned to the VLAN.
     */
    public readonly deviceId!: pulumi.Output<string>;
    /**
     * Add port back to the bond when this resource is removed. Default is
     * `false`.
     */
    public readonly forceBond!: pulumi.Output<boolean | undefined>;
    /**
     * Mark this VLAN a native VLAN on the port. This can be used only if this
     * assignment assigns second or further VLAN to the port. To ensure that this attachment is not first
     * on a port, you can use `dependsOn` pointing to another `equinix.MetalPortVlanAttachment`, just
     * like in the layer2-individual example above.
     */
    public readonly native!: pulumi.Output<boolean | undefined>;
    /**
     * UUID of device port
     */
    public /*out*/ readonly portId!: pulumi.Output<string>;
    /**
     * Name of network port to be assigned to the VLAN.
     */
    public readonly portName!: pulumi.Output<string>;
    /**
     * UUID of VLAN API resource
     */
    public /*out*/ readonly vlanId!: pulumi.Output<string>;
    /**
     * VXLAN Network Identifier.
     */
    public readonly vlanVnid!: pulumi.Output<number>;

    /**
     * Create a MetalPortVlanAttachment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: MetalPortVlanAttachmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: MetalPortVlanAttachmentArgs | MetalPortVlanAttachmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as MetalPortVlanAttachmentState | undefined;
            resourceInputs["deviceId"] = state ? state.deviceId : undefined;
            resourceInputs["forceBond"] = state ? state.forceBond : undefined;
            resourceInputs["native"] = state ? state.native : undefined;
            resourceInputs["portId"] = state ? state.portId : undefined;
            resourceInputs["portName"] = state ? state.portName : undefined;
            resourceInputs["vlanId"] = state ? state.vlanId : undefined;
            resourceInputs["vlanVnid"] = state ? state.vlanVnid : undefined;
        } else {
            const args = argsOrState as MetalPortVlanAttachmentArgs | undefined;
            if ((!args || args.deviceId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'deviceId'");
            }
            if ((!args || args.portName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'portName'");
            }
            if ((!args || args.vlanVnid === undefined) && !opts.urn) {
                throw new Error("Missing required property 'vlanVnid'");
            }
            resourceInputs["deviceId"] = args ? args.deviceId : undefined;
            resourceInputs["forceBond"] = args ? args.forceBond : undefined;
            resourceInputs["native"] = args ? args.native : undefined;
            resourceInputs["portName"] = args ? args.portName : undefined;
            resourceInputs["vlanVnid"] = args ? args.vlanVnid : undefined;
            resourceInputs["portId"] = undefined /*out*/;
            resourceInputs["vlanId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(MetalPortVlanAttachment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering MetalPortVlanAttachment resources.
 */
export interface MetalPortVlanAttachmentState {
    /**
     * ID of device to be assigned to the VLAN.
     */
    deviceId?: pulumi.Input<string>;
    /**
     * Add port back to the bond when this resource is removed. Default is
     * `false`.
     */
    forceBond?: pulumi.Input<boolean>;
    /**
     * Mark this VLAN a native VLAN on the port. This can be used only if this
     * assignment assigns second or further VLAN to the port. To ensure that this attachment is not first
     * on a port, you can use `dependsOn` pointing to another `equinix.MetalPortVlanAttachment`, just
     * like in the layer2-individual example above.
     */
    native?: pulumi.Input<boolean>;
    /**
     * UUID of device port
     */
    portId?: pulumi.Input<string>;
    /**
     * Name of network port to be assigned to the VLAN.
     */
    portName?: pulumi.Input<string>;
    /**
     * UUID of VLAN API resource
     */
    vlanId?: pulumi.Input<string>;
    /**
     * VXLAN Network Identifier.
     */
    vlanVnid?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a MetalPortVlanAttachment resource.
 */
export interface MetalPortVlanAttachmentArgs {
    /**
     * ID of device to be assigned to the VLAN.
     */
    deviceId: pulumi.Input<string>;
    /**
     * Add port back to the bond when this resource is removed. Default is
     * `false`.
     */
    forceBond?: pulumi.Input<boolean>;
    /**
     * Mark this VLAN a native VLAN on the port. This can be used only if this
     * assignment assigns second or further VLAN to the port. To ensure that this attachment is not first
     * on a port, you can use `dependsOn` pointing to another `equinix.MetalPortVlanAttachment`, just
     * like in the layer2-individual example above.
     */
    native?: pulumi.Input<boolean>;
    /**
     * Name of network port to be assigned to the VLAN.
     */
    portName: pulumi.Input<string>;
    /**
     * VXLAN Network Identifier.
     */
    vlanVnid: pulumi.Input<number>;
}
